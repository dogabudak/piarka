<Page loaded="loaded">
    <StackLayout>
        <Image src="res://logo" stretch="none" horizontalAlignment="center"/>

        <TextField text="{{ username }}" id="username" hint="Username" autocorrect="false" autocapitalizationType="none" />
        <TextField text="{{ password }}" secure="true" hint="Password" />
        <TextField text="{{ rePassword }}" secure="true" hint="Re-enter Your Password" />

        <Button text="Sign Up For TravelBuddy" tap="register" />
    </StackLayout>
</Page>
